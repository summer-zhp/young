<canvas type="2d" class="ec-canvas" id="{{canvasId}}"
  bindtouchstart="{{ec && !ec.disableTouch ? 'touchStart' : ''}}"
  bindtouchmove="{{ec && !ec.disableTouch ? 'touchMove' : ''}}"
  bindtouchend="{{ec && !ec.disableTouch ? 'touchEnd' : ''}}"
></canvas>
